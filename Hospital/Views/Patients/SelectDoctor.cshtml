@using X.PagedList
@model IPagedList<DAL.Models.DTO.DoctorDTO>

@{
    ViewData["Title"] = "Select Doctor";
}

<h1>Department: <strong>@ViewBag.DepartmentName</strong></h1>

<a href="javascript:history.back()" class="btn btn-secondary mb-3">
    <i class="fas fa-arrow-left"></i> Back
</a>

<div class="mb-3">
    <form id="searchForm" method="get">
        <input type="hidden" name="departmentId" value="@ViewBag.DepartmentId" />
        <div class="input-group">
            <input type="text" class="form-control" name="search" id="searchBox" placeholder="Search doctor..."
                   value="@ViewBag.Search" />
            <div class="input-group-append">
                <button class="btn btn-primary" type="submit">Search</button>
            </div>
        </div>
    </form>
</div>

<div id="doctorListContainer">
    @await Html.PartialAsync("~/Views/Patients/_DoctorListPartial.cshtml", Model)
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // 🔹 Handle Search Submit
        $("#searchForm").on("submit", function (e) {
            e.preventDefault();
            loadDoctors(1); // Reset to page 1 when searching
        });

        // 🔹 Handle Pagination Click
        $(document).on("click", ".pagination a", function (e) {
            e.preventDefault();
            const url = new URL($(this).attr("href"), window.location.origin);
            const page = url.searchParams.get("page");
            loadDoctors(page);
        });

        // 🔹 AJAX Load Function
        function loadDoctors(page) {
            const departmentId = "@ViewBag.DepartmentId";
            const search = $("#searchBox").val();

            $.ajax({
                url: '/PatientAppointment/SelectDoctor',
                data: { departmentId, search, page },
                type: 'GET',
                beforeSend: function () {
                    $("#doctorListContainer").html("<div class='text-center p-3'>Loading...</div>");
                },
                success: function (result) {
                    $("#doctorListContainer").html(result);
                },
                error: function () {
                    alert("Error loading doctor data!");
                }
            });
        }
    </script>
}
